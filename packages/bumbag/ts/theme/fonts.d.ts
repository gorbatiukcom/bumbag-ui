import { FontsThemeConfig } from '../types';
declare const _default: (overrides: FontsThemeConfig) => {
    importUrls?: string[];
    default: string;
    heading: string;
    mono: string;
};
export default _default;
